#include <stdlib.h>
#include <stdio.h>
#include <time.h>
/**
 * main -Entry
 * Return - 0 on sucess
 */
int main(void)
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch);
}
putchar('\n');
return (0);
}
