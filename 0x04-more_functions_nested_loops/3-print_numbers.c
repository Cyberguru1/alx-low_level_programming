#include "main.h"

/**
 * print_numbers - prints numbers within the range (0-0).
 * Return: retruns 0 on success
 */

void print_numbers(void)
{

for (int i = 0, i < 10, i++)
{
	_putchar(i);
}
_putchar('\n');
return (0);
}
