#include "main.h"
/**
 * _islower - program to check for lower palazo
 *  @c: contains value
 *  Retrun: 0 on palazo
 *  (code by cyberguru)
 */
int _islower(int c)
{
if (c > 'a' && c < 'z')
{
return (1);
}
else
{
return (0);
}
}
